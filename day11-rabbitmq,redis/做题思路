1.客户端:
  2.输入执行命令和执行命令的服务端的ip地址.(如果输入内容位'exit',就结束程序)
  3.连接本地主机的rabbitmq队列
  4.将输入字符串发送给每个服务端
	5.如果执行操作是'run':
	   6.则发送输入字符串的同时将taskID一起发送给服务端.
	   7.输出taskID.
	8.如果执行操作是'check_task':
	   9.接受服务端返回的执行结果.
	   10.输出执行结果
	11.回到步骤2.
12.服务端:
   13.连接客户端的rabbitMQ
   14.如果执行命令是'run':
	15.取出其中的命令,执行命令,将命令结果和taskID以值键的形式存储起来.
   16.如果执行命令是'check_task':
	17.取出其中的taskID,将字典中相应的值取出.
	18.将命令结果和taskID放到回复队列中.
